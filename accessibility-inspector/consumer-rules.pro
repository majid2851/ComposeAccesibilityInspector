# Consumer ProGuard rules for compose-accessibility-inspector.
# The library is intended for debug builds and adds no runtime cost to release
# builds when gated behind BuildConfig.DEBUG. No special keep rules are required.
